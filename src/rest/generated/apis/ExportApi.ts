/* tslint:disable */
/* eslint-disable */
/**
 * Observe API
 * # Overview Welcome to the Observe API Docs!  The Observe API is a RESTful API. POST bodies and responses are typically JSON-encoded.  ## Authentication Requests to the Observe API are authenticated with an API token. The token can be supplied using HTTP Bearer authentication: ``` // passing token as bearer token curl -H \"Authorization: Bearer <customerid> <token>\" https://<customerid>.<observe-domain>.com/v1/... ``` 
 *
 * The version of the OpenAPI document: 1.0.230307
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ExportQueryRequest,
} from '../models/index';

export interface ExportApiExportQueryOperationRequest {
    exportQueryRequest: ExportQueryRequest;
    startTime?: string;
    endTime?: string;
    interval?: string;
    stage?: string;
    paginate?: boolean;
}

export interface ExportApiExportQueryPageRequest {
    cursorId: string;
    offset?: string;
    numRows?: string;
}

export interface ExportApiExportWorksheetRequest {
    worksheetId: number;
    startTime?: string;
    endTime?: string;
    interval?: string;
    stage?: string;
}

/**
 * 
 */
export class ExportApi extends runtime.BaseAPI {

    /**
     * Execute an OPAL query.  Results can be returned as CSV or NDJSON. * Set Accept header to `text/csv` or `application/x-ndjson` * Default: CSV  Either two of `startTime`, `endTime`, and `interval` or `interval` alone can be specified to set the time interval. The `startTime` parameter is inclusive and the `endTime` parameter is exclusive, to prevent overlap from subsequent windows.  * `interval`: An interval relative to now * `startTime` + `endTime`: The specified time interval * `startTime` **or** `endTime` + `interval`: The specified time interval relative to the provided start or end time  Inputs are specified as dataset IDs, or previously-defined stages.  Set paginate to true for an asynchronous paginated mode. The response will be 202 Accepted and contain headers for fetching the results via `/v1/meta/export/query/page`. You may also want to increase rowCount to allow large, paginated queries. 
     * Execute an OPAL query
     */
    async exportQueryRaw(requestParameters: ExportApiExportQueryOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<string>> {
        if (requestParameters['exportQueryRequest'] == null) {
            throw new runtime.RequiredError(
                'exportQueryRequest',
                'Required parameter "exportQueryRequest" was null or undefined when calling exportQuery().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['startTime'] != null) {
            queryParameters['startTime'] = requestParameters['startTime'];
        }

        if (requestParameters['endTime'] != null) {
            queryParameters['endTime'] = requestParameters['endTime'];
        }

        if (requestParameters['interval'] != null) {
            queryParameters['interval'] = requestParameters['interval'];
        }

        if (requestParameters['stage'] != null) {
            queryParameters['stage'] = requestParameters['stage'];
        }

        if (requestParameters['paginate'] != null) {
            queryParameters['paginate'] = requestParameters['paginate'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/meta/export/query`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: requestParameters['exportQueryRequest'],
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<string>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Execute an OPAL query.  Results can be returned as CSV or NDJSON. * Set Accept header to `text/csv` or `application/x-ndjson` * Default: CSV  Either two of `startTime`, `endTime`, and `interval` or `interval` alone can be specified to set the time interval. The `startTime` parameter is inclusive and the `endTime` parameter is exclusive, to prevent overlap from subsequent windows.  * `interval`: An interval relative to now * `startTime` + `endTime`: The specified time interval * `startTime` **or** `endTime` + `interval`: The specified time interval relative to the provided start or end time  Inputs are specified as dataset IDs, or previously-defined stages.  Set paginate to true for an asynchronous paginated mode. The response will be 202 Accepted and contain headers for fetching the results via `/v1/meta/export/query/page`. You may also want to increase rowCount to allow large, paginated queries. 
     * Execute an OPAL query
     */
    async exportQuery(requestParameters: ExportApiExportQueryOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<string | null | undefined > {
        const response = await this.exportQueryRaw(requestParameters, initOverrides);
        switch (response.raw.status) {
            case 200:
                return await response.value();
            case 202:
                return null;
            case 206:
                return await response.value();
            default:
                return await response.value();
        }
    }

    /**
     * Fetch the results of a paginated \"/v1/meta/export/query\" request.  Note that paginate must be set to true and rowCount should be set high enough to contain the total expected result set.  This endpoint uses long polling behavior. If a query is in progress when a request is made, the server will delay responding for up to 30 seconds. If the query completes in that time period, the results will be returned with a 200 response code. Otherwise a 202 response code will be returned, and the client should retry the request. The endpoint will also return an error if the query failed.  Queries for paginated data will return an `X-Observe-Total-Rows` header and an `X-Observe-Next-Page` header with a URL for the next page, as long as one is needed. You can use the `X-Observe-Cursor-Id` and the `offset` and `numRows` parameters to construct your own next page URL with different page sizes.  The results can be returned as CSV or NDJSON. The query parameters are used to specify which page of results to fetch. * Set Accept header to `text/csv` or `application/x-ndjson` * Default: CSV  The number of rows in the full result set is returned in the X-Observe-Total-Rows response header. 
     * Fetch a page of query results
     */
    async exportQueryPageRaw(requestParameters: ExportApiExportQueryPageRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<string>> {
        if (requestParameters['cursorId'] == null) {
            throw new runtime.RequiredError(
                'cursorId',
                'Required parameter "cursorId" was null or undefined when calling exportQueryPage().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['cursorId'] != null) {
            queryParameters['cursorId'] = requestParameters['cursorId'];
        }

        if (requestParameters['offset'] != null) {
            queryParameters['offset'] = requestParameters['offset'];
        }

        if (requestParameters['numRows'] != null) {
            queryParameters['numRows'] = requestParameters['numRows'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/meta/export/query/page`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<string>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Fetch the results of a paginated \"/v1/meta/export/query\" request.  Note that paginate must be set to true and rowCount should be set high enough to contain the total expected result set.  This endpoint uses long polling behavior. If a query is in progress when a request is made, the server will delay responding for up to 30 seconds. If the query completes in that time period, the results will be returned with a 200 response code. Otherwise a 202 response code will be returned, and the client should retry the request. The endpoint will also return an error if the query failed.  Queries for paginated data will return an `X-Observe-Total-Rows` header and an `X-Observe-Next-Page` header with a URL for the next page, as long as one is needed. You can use the `X-Observe-Cursor-Id` and the `offset` and `numRows` parameters to construct your own next page URL with different page sizes.  The results can be returned as CSV or NDJSON. The query parameters are used to specify which page of results to fetch. * Set Accept header to `text/csv` or `application/x-ndjson` * Default: CSV  The number of rows in the full result set is returned in the X-Observe-Total-Rows response header. 
     * Fetch a page of query results
     */
    async exportQueryPage(requestParameters: ExportApiExportQueryPageRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<string | null | undefined > {
        const response = await this.exportQueryPageRaw(requestParameters, initOverrides);
        switch (response.raw.status) {
            case 200:
                return await response.value();
            case 202:
                return null;
            default:
                return await response.value();
        }
    }

    /**
     * Export data used by a worksheet.  Results are limited to a maximum of 100,000 rows.  Results can be returned as CSV or NDJSON. * Set Accept header to `text/csv` or `application/x-ndjson` * Default: CSV    Either two of `startTime`, `endTime`, and `interval` or `interval` alone can be specified to set the time interval. The `startTime` parameter is inclusive and the `endTime` parameter is exclusive, to prevent overlap from subsequent windows.  * `interval`: An interval relative to now * `startTime` + `endTime`: The specified time interval * `startTime` **or** `endTime` + `interval`: The specified time interval relative to the provided start or end time 
     * Export a worksheet
     */
    async exportWorksheetRaw(requestParameters: ExportApiExportWorksheetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<string>> {
        if (requestParameters['worksheetId'] == null) {
            throw new runtime.RequiredError(
                'worksheetId',
                'Required parameter "worksheetId" was null or undefined when calling exportWorksheet().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['startTime'] != null) {
            queryParameters['startTime'] = requestParameters['startTime'];
        }

        if (requestParameters['endTime'] != null) {
            queryParameters['endTime'] = requestParameters['endTime'];
        }

        if (requestParameters['interval'] != null) {
            queryParameters['interval'] = requestParameters['interval'];
        }

        if (requestParameters['stage'] != null) {
            queryParameters['stage'] = requestParameters['stage'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/meta/export/worksheet/{worksheetId}`;
        urlPath = urlPath.replace(`{${"worksheetId"}}`, encodeURIComponent(String(requestParameters['worksheetId'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<string>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Export data used by a worksheet.  Results are limited to a maximum of 100,000 rows.  Results can be returned as CSV or NDJSON. * Set Accept header to `text/csv` or `application/x-ndjson` * Default: CSV    Either two of `startTime`, `endTime`, and `interval` or `interval` alone can be specified to set the time interval. The `startTime` parameter is inclusive and the `endTime` parameter is exclusive, to prevent overlap from subsequent windows.  * `interval`: An interval relative to now * `startTime` + `endTime`: The specified time interval * `startTime` **or** `endTime` + `interval`: The specified time interval relative to the provided start or end time 
     * Export a worksheet
     */
    async exportWorksheet(requestParameters: ExportApiExportWorksheetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<string> {
        const response = await this.exportWorksheetRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
